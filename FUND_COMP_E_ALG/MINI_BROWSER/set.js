// Ele evita adicionar textos iguais, mesmo que forçado
var novoSet = new Set();

novoSet.add('backend');
novoSet.add('frontend');
novoSet.add('mobile');

var resultado = novoSet;
console.log(resultado);